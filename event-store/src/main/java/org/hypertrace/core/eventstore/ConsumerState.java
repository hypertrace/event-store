package org.hypertrace.core.eventstore;

/**
 * Marker interface to store the consumer offsets per partition. Each Consumer Type will have its
 * own way to store the offsets
 */
public interface ConsumerState {

}
